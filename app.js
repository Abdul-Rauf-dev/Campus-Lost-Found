// ================================================================
//  Campus Lost & Found — app.js  (USER PANEL)
//
//  HTML and CSS are ready. Build the JavaScript here.
//  Open DevTools Console (F12) before you start.
//  Make sure JSON Server is running: json-server --watch db.json
// ================================================================
const API_URL =" https://localhost:3000/reports";
const StatusmsgEl  = document.getElement By Id ("status-msg");
const reportList = document.getElement ById("report-list");
